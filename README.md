# AcademicPotentialK23DJ
An API using Artificial Neural Networks will predict a course's employment potential. It uses a parameter-tuned Multilayer Perceptron model, trained and tested in the backend, while users interact through a web-based frontend.
